# Introdução aos Testes no Frontend

Intalação da Jest e React Testing Library:

```
yarn add -D jest @types/jest jest-environment-jsdom @testing-library/react @testing-library/jest-dom ts-jest jest-css-modules
```